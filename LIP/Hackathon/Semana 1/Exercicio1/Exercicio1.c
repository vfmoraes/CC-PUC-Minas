#include <stdio.h>

int main(){

  int a, b, diferenca;

  puts("Digite os valores de A e B para serem subtraídos");
  scanf("%i %i", &a, &b);
  diferenca = a - b;
  printf("DIFERENCA = %i", diferenca);

}